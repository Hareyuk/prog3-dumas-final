import { ContainerDiv } from './styles'

const ContWidthFull = ({children}) => {
    return (
        <ContainerDiv>
            {children}
        </ContainerDiv>
    )
}

export default ContWidthFull;