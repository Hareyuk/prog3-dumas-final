import { ContainerDiv } from './styled'

const ContWidthFull = (BColor, {children}) => {
    return (
        <ContainerDiv>
            {children}
        </ContainerDiv>
    )
}

export default ContWidthFull;